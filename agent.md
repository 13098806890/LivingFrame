# Agent Instructions

- 默认不要主动执行构建、测试或其他验证命令。
- 只有用户明确要求构建、运行测试或验证时，才执行对应操作。
- 除非用户明确修改当前版本号，否则只更新当前版本的 release note，不新增版本号。

## 测试编排策略

- 涉及 UI、交互、可用性或体验审计的任务，默认采用“先探索、后固化”：先由编排 Agent 指挥 tester 通过实时 UI/计算机操作通道，把每一步的新截图、AX 元素树和相关页面代码上下文交给 LLM，由 LLM 决定下一步单个动作，再执行并读取新状态。探索阶段记录可复现路径、异常状态、截图、AX 证据和候选稳定选择器。
- 探索阶段禁止使用 XCUITest 方法、预写 selector、断言驱动流程或批量脚本来代替 LLM 的实时决策；代码分析只能辅助解释当前页面，不能替代真机交互证据。
- 探索证据经编排 Agent 核对后，才把确认过的路径交给 Developer 固化为可重复的 XCUITest/回归测试；固定测试不能反向冒充探索已完成。
- 探索通道或指定真机不可用时，必须明确标记探索阶段为 blocked，不得静默改成只跑固定脚本。用户明确要求 fixed-only、纯代码/单元测试或发布合规任务时，编排 Agent 可跳过探索，但要记录跳过原因和残余风险。
