# GIFBloom Release Notes

## 1.0 (Build 7) — 2026-09-22

### 预览播放启动与本地化修复（2026-09-23）

- 动态素材预览改为滑动窗口解码：先加载前几帧即可开始播放，后续帧在后台分批预加载，避免一次性解码整段素材造成长时间等待和额外内存占用。
- 素材详情页、提取页和素材选择卡片在准备播放期间显示明确的加载状态，播放按钮与画布状态保持同步。
- 修复提取页面英文模式下“剪影素材”摘要缺少对应格式化本地化 key 而回退显示中文的问题，并完成 17 个语言包的 key、占位符和重复项检查。

### 购买状态、提取稳定性与本地化清理

- 应用回到前台时主动刷新订阅和永久买断权益；已有订阅的用户仍可购买永久版，买断完成后不再显示订阅入口。
- 加固人物提取的帧率参数、免费版进度时长和失效音频引用，避免异常参数、过期素材清单或缓存数据导致错误状态。
- 删除已下线 SAM2/Emoji 资源相关的无效依赖和本地化 key，保留当前三类功能入口所需文案；17 个语言包和 InfoPlist 本地化 key 已完成一致性校验。

### 拼接循环与贴纸效果优化（2026-09-22）

- 拼接页面新增“对齐到最长”循环选项；动态素材默认持续循环到工程中最长素材结束，也可以切换回固定循环次数。
- 优化贴纸漫画效果的白色边缘和阴影表现，减少人物位置偏移并增强贴纸层次感。

## 1.0 (Build 7) — 2026-09-16

### 统一画布背景设置（2026-09-19）

- 移除视频、贴纸、特效等素材检查器中的独立背景图案设置入口，背景颜色、图案和画布外缘统一从“画布”面板调整。
- 贴纸检查器仅保留贴纸风格、滤镜和播放控制，不再提供背景设置。
- 同时移除元素级背景图案的数据和渲染逻辑；旧工程中的元素级背景图案不再保留或应用，不提供向后兼容。

### 设置页与 UI 审计流程（2026-09-19）

- 新增设置页完整视口采集审计：按半屏重叠滚动保存截图和无障碍层级，并记录 Pro 订阅页的 StoreKit、系统权限提示和可用性状态。
- UI 审计流程明确采用“先实时探索、再固化测试”的顺序；探索通道不可用时记录为 blocked，避免固定脚本替代真实交互证据。

### 动态水果贴纸、AI 墨镜与临时文件清理（2026-09-18）

- 新增多款动态水果贴纸，以及可根据选中人物五官和头部角度调整的 AI 墨镜贴纸。
- AI 贴纸可从多个剪影素材中选择目标，并改善透明贴纸选中框与跟随缩放稳定性。
- 导入完成后清理不再使用的临时源文件；应用启动时自动清除上次运行遗留超过一天的 `LF-*` 临时文件。

### App 图标配色主题优化（2026-09-17）

- 校准晴空蓝、奶油白与珊瑚橙的主题色层级，让应用界面更贴近 GIFBloom 图标，同时保留按钮、标题和状态文字所需的对比度。
- 更新设置页主题预览，直接展示图标中的品牌橙色。

### Emoji 风格贴纸（2026-09-17）

- 新增 8 款小黄脸静态贴纸：微笑、笑哭、爱心眼、眨眼、大哭、思考、惊讶和生气，沿用现有贴纸资源目录与选择器。
- 贴纸预览支持单帧图稿，并补充 Twemoji 的 CC BY 4.0 来源署名。

### GIFBloom Logo 动态贴纸（2026-09-16）

- 新增 5 款透明 GIFBloom Logo 动态贴纸：气泡、手写、渐变、涂鸦和蜡笔风格，统一归入 `logo` 分类，贴纸标题统一显示为“logo”。
- 移除贴纸选择器中的“蜡笔绘本”分类，保留原有“涂鸦”贴纸。
- 统一 Core Package 资源包与 App `Watermarks` 目录的贴纸资源解析，确保缩略图预览、画布渲染和导出使用同一套 GIF 资源。
- 增加 Logo 贴纸目录与完整帧加载的回归测试。

### 编辑器弹层审计与测试数据隔离（2026-09-16）

- 增加编辑器菜单、确认弹窗、素材选择、画布、文字、导出、帧编辑、检查器和贴纸预览等界面的 UI 审计测试，并采集截图与无障碍层级。
- 真机审计夹具改用每次运行独立的缓存目录；检测到隔离配置缺失时会停止测试，避免写入用户作品、素材或设置。
- 扩展拼接编辑器播放次数菜单与源片段范围面板的 UI 回归断言。

### 简化产品结构

- 移除 Widget 扩展及其设置页面。Widget 只能展示作品静态首帧，无法播放 GIF、视频或动态照片，继续保留会增加用户理解和维护成本。
- 移除 Widget 专用的 poster 存储、App Group 权限和导出后同步逻辑。
- 清理工程 target、相关本地化文案及旧的 Widget 配置，主 App 不再打包无实际动态播放能力的小组件。

## 1.03 (Build 6) — 2026-08-26

### 检查器入口与效果预览优化（2026-09-15）

- 在画布或时间轴选中内容后提供明确的“调整”入口，打开对应检查器更直观。
- 风格和滤镜选项缩略图统一显示编辑器当前帧，并共用剪影主体识别与缩放比例，避免两组选项中的素材大小不一致。
- 滤镜预览先按原始剪影确定取景，再叠加描边、风格和背景图案，避免已有视觉效果改变主体缩放。
- 增加风格与滤镜缩略图主体范围一致性的回归测试。

### 素材库与文件夹操作优化（2026-09-15）

- 素材提取和拼接素材选择支持一次多选；所选项目会在用户完成选择后统一导入，不再点选一项就提前关闭。
- 文件夹支持批量添加和批量移除素材，缩略图显示选择状态；移除只改变文件夹归属，不会删除素材原件。
- 素材库缩略图增加删除入口，并保护仍被已保存作品引用的素材；文件夹内也可快速移除单个素材。
- 统一缩略图四角操作的位置与触控尺寸：删除、帧/边缘状态、播放和音频标志分别对齐在四角。
- 素材详情页的顺时针、逆时针旋转操作移至预览画布上，使用轻量图标按钮。
- 文件夹内的删除入口现在会真正删除素材并二次确认；若素材仍被作品引用，会提示先解除引用。单独的“移出文件夹”仍只改变归属，不删除素材。
- 修正缩略图删除按钮与打开详情/播放手势的命中冲突，避免点删除无响应或误打开素材。

### 编辑画布纹理与素材用语统一（2026-09-15）

- 画布设置、画布检查器和素材背景设置共用同一套平行线/马赛克控件；分别写入画布背景或素材自身，不混淆数据作用域。
- 重整纹理选项布局：样式使用带图标的卡片，参数按线条粗细、线条间距、方块大小、颜色和角度分组；马赛克不显示无关角度选项。
- “横线”更名为“平行线”；新增参数说明与 16 种语言的本地化。
- 扩大透明背景色块的有效点击区域，让棋盘格显示层不再影响按钮命中。
- 全应用中文素材处理术语统一为“剪影”，英文统一使用 “Cutout”，覆盖提取、设置、文件夹、错误提示和无障碍文案。

### 提取页面 UX 修复（2026-09-15）

- 区分素材导入失败与人物素材生成失败，错误标题、说明和恢复操作现在保持一致。
- “选择其他素材”会清理上一次失败素材和错误状态，并将辅助功能焦点返回提取入口，避免误重试旧素材。
- 优化超大字体（XXXL）下提取页的底部安全区，入口、设置摘要、进度/错误状态和素材区域不会被底部 TabBar 遮挡。
- 增加中文/英文错误分类与 XXXL 安全区回归测试；指定 iPhone 17 Pro Max Simulator 定向回归 7/7 通过。
- 用户可见品牌统一为 GIFBloom；本次未改动内部工程 target/module/path 等技术标识。

### 设置页开关与拼接编辑修复（2026-09-14）

- 修复“保留原始帧率和分辨率”开关点击后无响应的问题：拆分设置标签与实际 Toggle 控件，避免辅助功能节点命中错误导致开关状态不切换。
- 设置开关现在会正确联动处理分辨率/帧率选项，并在应用重启后保持用户选择。
- 补充 Settings 与拼接编辑器的 UI 回归测试、Simulator 截图、可访问性层级和功能审计证据。
- 修复拼接编辑器重新打开时的素材/布局恢复，以及多素材裁剪、移动、旋转相互串改的问题；静态和动态素材的播放控制与预览时间范围保持一致。

### 作品保存、素材旋转与导出帧率（2026-09-14）

- 草稿与正式作品状态改为显式记录，重复自动保存不会再把仅有草稿的工程误列为已保存作品。
- 自动保存会在封面渲染后再次校验工程快照和作品身份；最新草稿策略在串行持久化流程中基于磁盘状态执行，降低工程切换或并发保存时写入过期草稿的风险。
- 编辑器保存状态只会在当前修改确实写入草稿后显示“草稿已保存”；防抖等待期间会明确显示仍有未保存修改。
- 作品清单和封面使用原子写入，读取或解码失败会记录诊断日志；保存当前作品后才清理旧草稿，减少中断时丢失最新草稿的风险。
- 素材详情页支持顺时针和逆时针旋转 90°；旋转动画可连续经过 360°，并保持裁剪区域与原素材坐标一致。
- 导出帧率提供 10、15、30、60 fps 常用档位，并按工程动态素材的最高源帧率限制可选上限；GIF 详情导出仍使用适合 GIF 的帧率档位。
- 修正旋转后裁剪编辑器的画幅计算，并避免 GIF 规格估算完成后覆盖用户手动选择的规格。

### 素材详情页、裁剪与透明 GIF 导出（2026-09-14）

- 素材详情页改为全屏展示：左上角删除素材、右上角完成；删除确认弹窗固定提供“删除 / 取消”两个操作。
- 新增素材裁剪：详情页可直接框选保留区域，裁剪坐标跟随素材旋转，预览、编辑与导出使用同一裁剪结果。
- 修正素材旋转 90° 后的详情页预览尺寸：按旋转后的宽高布局，不再出现素材突然放大或详情页比例异常。
- 透明 GIF 导出规格改为按源素材动态过滤：分辨率提供 360p、720p 和最高源分辨率，帧率提供 15、30 和最高源帧率；超出源分辨率的档位不会出现，也不会放大素材。
- 分辨率选项直接显示实际数值（例如 900p、1080p），不再统一显示“最高”。
- 导出大小估算扣除 GIF 容器固定开销，估算值更接近实际导出体积；默认选择估算值不超过 10 MB 且最接近 10 MB 的组合。
- 详情页导出完成后自动保存到相册并显示成功状态；旋转、裁剪或修改导出规格后会重置为可再次导出。
- 编辑页给素材设置的描边、贴纸等效果不再影响素材详情页的透明 GIF 导出，导出始终使用原始抠图内容。

### 素材提取与方向修正（2026-09-14）

- 素材提取完成后自动打开刚生成的素材详情页；批量提取时打开最后一个成功生成的素材，避免连续弹出多个页面。
- 视频轨旋转方向优先于静态图 EXIF 方向，修复部分视频/Live Photo 提取后素材倒置的问题。

### 导出页与素材库维护（2026-09-14）

- 编辑页导出分辨率选项按当前工程素材动态过滤，不再显示无法达到的档位。
- 素材库文件夹“新建”入口与已有文件夹合并为同一个横向滚动区域，保持紧凑布局。
- 补充 GIF 规格与估算、素材裁剪与旋转、视频方向、渲染效果隔离、素材预览缓存等单元测试。

### Simulator 自动巡检与功能回归（2026-09-12）

- 新增独立 UI 测试 Target 和一键巡检脚本，可自动启动指定 iOS Simulator、构建 App、运行测试并归档截图、运行日志、可访问性层级和 xcresult。
- 增加确定性的 Debug 测试工程，在不依赖系统相册内容和照片权限的情况下，覆盖素材落盘与画布渲染、动态贴纸播放、全屏预览、画布设置、文字编辑和贴纸添加。
- 功能巡检继续覆盖手动保存、作品列表、重新打开、真实 GIF 编码，以及主题和素材质量设置的重启持久化。
- 完整巡检同时覆盖简体中文、英文、阿拉伯语、深色外观和无障碍超大字体，并使用带原始截图的离线中文 HTML 输出巡检结论。

### 拼接编辑器交互与图层命中（2026-09-10）

- 点击拼接画布中的图片时，按图片经过缩放、旋转和位移后的实际显示范围命中，不再只按分区命中最上层素材。
- 同一区域中的图片分别移动到不同位置后，可以直接点击对应图片进行选择；图片真正重叠时仍按图层顺序优先命中上层。
- 点击空白区域只聚焦区域，添加和移除素材改为通过明确的操作按钮完成，避免误触删除当前素材。
- 增加图层选择菜单和清晰的当前图层状态，可直接选择任意图层，并使用“上移 / 下移”调整前后顺序。
- 新增素材不再自动加入区域；素材拖动过程中即使短暂停顿，也会持续到手指抬起，避免拖动中途失效。

### 入口命名、拼接与代码整理（2026-09-09）

- 将编辑器中的“提取”入口统一命名为“人物”，选择页、人物处理设置和视频范围确认文案同步调整。
- 修复编辑器点击“拼接”后没有立即弹出选择器、需要再次点击画布才显示的问题。
- 统一背景分区的预览、命中测试和导出几何计算，补充分区覆盖测试，修复 Core Image 坐标系方向不一致。
- 抽取相册媒体导入和素材处理服务，删除废弃的背景选择页面并拆分素材选择卡片。
- 渲染器改为依赖背景媒体提供协议；作品写盘、缓存维护等职责从 AppState 中拆出，降低模块耦合。

### 编辑保存、草稿与检查器体验（2026-09-09）

- 正式作品改为手动点击“保存”后才更新；编辑过程自动保存为独立草稿，不覆盖正式作品。
- 作品页增加草稿箱，草稿与正式作品分组展示，并显示草稿最近更新时间；重新打开作品时可继续编辑草稿或恢复正式版本。
- 文字、静态图片、静态背景和静态贴纸/效果支持在时间轴上调整起点与时长；静态元素不会改写动态素材的源片段范围。
- 背景检查器移除暂不成熟的分割边缘入口，保留底层数据和渲染能力，后续优化后可恢复。
- 统一检查器 header、footer 和选中态样式，减少重复标题层级；背景分区预览和拖动手柄使用统一高亮反馈。
- 新增 Inter 与思源黑体常规字体资源，字体预览固定使用 `text` 示例并支持键盘收起；移除重复的粗体字体选项。

### 导出、日志与稳定性维护（2026-09-09）

- Live Photo 导出使用 Photos 可识别的独立配对资源，并补充 `xdz.livephoto` 关键链路日志。
- 设置页清空调试日志改为后台异步执行，完成后再刷新界面，避免大量日志操作造成 UI 卡顿。
- 清理 App Icon 资源目录中未分配的概念图，避免 Xcode 资源警告。

### 画布选中框与素材旋转（2026-09-09）

- 选中框统一复用渲染器生成的完整素材矩形，不再针对普通素材、文字、贴纸和特效分别估算尺寸。
- 修复素材旋转 90°/270° 后选中框仍使用原始宽高的问题，并同步修正旋转素材的渲染缩放。
- 统一选中框的缩放、坐标映射和定位算法，补充几何计算单元测试。

### 时间轴循环与画布偏好（2026-09-09）

- 明确区分检查器源范围与时间轴播放窗口：时间轴裁剪只改变首轮播放偏移和播放时长，不改写每轮循环使用的源入点/出点。
- 左侧时间轴手柄向右移动时首轮从后续帧开始，向左扩展时按循环范围回绕；例如源帧 `1,2,3,4,5` 向左扩展两帧后播放为 `4,5,1,2,3,4,5`。
- 循环回绕统一从检查器源范围起点开始，首轮偏移后的循环分割线按首轮剩余长度和完整循环长度准确定位。
- 新建画布会记住用户上一次选择的画布比例与背景设置，并通过 `UserDefaults` 持久化；重新打开已有作品时继续使用作品自身配置。

### 导出质量与透明工作流（2026-09-08）

- GIF、HEVC-alpha 透明视频和 H.264 视频支持在导出前选择原始、480p、720p 或 1080p 分辨率，以及 10、15 或 30 fps。
- 导出页增加当前帧输出预览，明确显示实际输出像素、帧率和总帧数；导出完成后显示准确文件体积，避免为估算体积提前编码完整动画。
- 视频码率会随导出分辨率和帧率匹配调整，低清预设不再沿用高码率造成不必要的大文件。
- GIF 帧数改为向上取整，保留片段末尾不足一帧的时长；微信 GIF 保留 240 与 720 两档快捷预设，超出 10 MB 时仅均匀抽帧，不降低已选画面尺寸。
- 透明背景可在画布背景中直接选择；HEVC-alpha 导出使用原生 alpha 编码路径，避免不受支持的压缩属性导致导出崩溃。

### 动态素材时间轴与贴纸

- 视频素材、动态背景、动态贴纸和效果统一使用源范围与显式播放次数模型：默认播放一次，裁剪后可先恢复完整素材，再由检查器明确设置重复次数。
- 时间轴未选中时也会清楚显示源范围与有效播放范围；检查器打开时自动暂停预览，避免播放状态下无法稳定修改参数。
- 新增简约时钟涂鸦贴纸，并清理不参与运行时的生成过程参考图。

### 稳定性与性能维护（2026-09-08）

- 统一视频素材、动态贴纸和动态背景的源素材范围模型，时间轴裁剪、帧缩略图、编辑预览和导出使用同一套入点/出点计算。
- 修复时间轴选区外遮罩与素材帧带可能错位的问题，并保持未播放区域的灰度提示和选中区域的正常亮度。
- 作品保存、复制、重命名和删除改为串行后台持久化，避免快速连续操作时旧数据覆盖新数据。
- 素材删除先从内存和磁盘扫描范围隔离，再异步清理大量帧文件，减少删除卡顿和应用重启后素材复现风险。
- 背景媒体元数据和素材缓存大小改为缓存/后台计算，降低编辑页和设置页重复扫描磁盘造成的卡顿。
- 清理缓存与异步清单写入增加顺序保护，避免清空素材后旧的后台写入重新生成清单。
- 作品列表改为后台加载并显示加载状态，素材属性更新统一复用同一套持久化与刷新逻辑。

### 文字编辑与排版

- 文字预览和导出统一使用 CoreText 实际字形边界，修复文字框比内容短、顶部留白过大的问题。
- 修正文字从 CoreText 到 Core Image 的坐标转换，避免文字被重复镜像或旋转 180°。
- 编辑工具栏使用本地化文字入口：中文显示“文字”，英文显示“Text”。
- 文字编辑面板与元素检查器共用同一套控件，支持字号、系统字体/可用字体选择和自定义任意文字颜色。
- 移除编辑器中的特效工具入口，保持核心编辑流程简洁。

### 素材库恢复

- 素材库启动恢复不再因为旧清单中缺少可选字段或存在旧边缘样式值而静默跳过整个素材目录。
- 已存在磁盘上的素材会使用当前默认参数完成恢复，避免升级后素材列表异常减少。

### 视频素材提取

- 设置页增加单个素材最大提取时长，可选择 3、5、8 或 10 秒，默认 5 秒。
- 动态提取超过 1 分钟的视频时，先进入视频范围选择界面，再开始抠图；用户可以在缩略图时间轴上调整起点、终点或整体移动选区。
- 视频范围选择界面支持预览当前选中的片段，播放到选区终点后自动停止；选区外内容降低透明度，最大提取时长始终受到设置限制。
- 素材库动态素材的播放按钮扩大触控区域，并与素材拖拽入口分离，避免素材尚未完成解码时误触发文件夹拖拽。

### 画布边框图层

- 画布手撕/卷角边框正式作为独立透明图层加入时间轴，可像其他元素一样调整上下层级。
- 编辑预览与导出统一按照时间轴中的真实图层顺序合成；边框不再固定覆盖所有素材，位于边框上方的素材可以正常覆盖边缘。
- 兼容旧作品：打开旧工程时自动补齐画布边框图层，并保留原有边框样式。
- 修复相纸九宫格渲染在四角出现垂直细线的拼接缝问题。

### 主题与编辑器界面

- 增加四套明快的马卡龙主题，并将时间轴、按钮、素材和贴纸颜色统一接入主题语义色。
- 移除素材页面重复的右上角添加按钮，减少重复入口。
- 优化边框图层在时间轴和检查器中的选择、排序与显示。

### 编辑器与渲染稳定性

- 选中素材时增加统一的蓝色高亮边框、控制点和时间轴有效范围提示，裁剪手柄保留更大的触摸热区。
- 导出流程增加分阶段内存诊断与渲染缓存回收，降低高分辨率、多帧导出时的内存峰值。
- 纸张边缘改用透明相纸素材叠加，保留手撕纹理、阴影和卷角的不透明区域，并修正不同画幅下的比例适配。
- 补充纸张边缘合成快照测试，确保编辑预览和导出使用一致的边缘效果。

### 时间轴工作区

- 时间轴改为固定工作区：左侧轨道列表与右侧时间内容同步纵向滚动，缩放到 200% 后可直接横向浏览，不再依赖难以命中的滚动条。
- 增大左右裁剪手柄的实际触控范围，并为左裁剪、右裁剪和整体移动保留独立拖拽状态；右侧裁剪不会带动素材起点。
- 时间轴为右侧系统滚动条预留间距，轨道拉满或数量较多时仍可上下滚动、选择和排序素材。
- 选中素材后可从时间轴顶部直接删除，删除前使用系统确认框；时间刻度、播放头与素材条布局进一步对齐。

### 动态背景与分区

- Live Photo、视频和 GIF/APNG 动态背景默认使用自身完整播放时长，并按真实帧时长取帧。
- 修复背景分区修改后编辑预览未立即刷新的问题；删除其他素材后才更新的延迟现象不再出现。
- 点击未被上层分区覆盖的区域时会选中实际可见的下层素材；分区默认角度统一为 90°。
- 调整背景位置、缩放、旋转和分区参数时统一触发画布重渲染，预览与导出继续使用同一合成结果。

### 画布纸张边缘

- 增加柔和相纸、纤维手撕和卷角相纸三种画布边缘，分别对应参考设计中的 A、B、C 风格。
- 纸纹、非对称撕裂轮廓、接触阴影和投影由透明素材合成；卷角相纸保留后层纸张、右侧翘起和长阴影。
- 柔和相纸和纤维手撕使用九宫格适配不同画幅，卷角相纸保持完整图层以避免卷曲与阴影产生拼接缝。
- 纸张渲染结果按画布参数缓存，并由编辑预览和导出共同使用。

### 代码整理与时间轴性能

- 清理时间轴遮罩排查期间遗留的 `xdz` 控制台日志、View 层级递归诊断和无用状态，避免拖动时产生额外开销。
- 视频范围选择页改为异步加载并缓存视频画幅比例，避免 SwiftUI 重绘时重复读取媒体轨道。
- 编辑时间轴统一抽取素材源范围计算，完整显示素材帧带，并将实际播放区间与未播放区间分离渲染。
- 优化视频范围时间轴的手势协调：水平拖动用于调整选区，纵向手势交给外层滚动容器处理。
- 统一编辑器选中态颜色 token、导航标题样式和页面背景样式，移除不再使用的主题别名与循环缩略图组件。

## 1.02 (Build 4) — 2026-08-24

### 多图动态背景与分区

- 可从相册添加静态图片、GIF/APNG 与 Live Photo 动态背景；动态原始资源会在需要时下载并显示导入进度。
- 多张背景可作为独立元素叠放并进入时间轴，静态背景不会再显示错误的循环标记。
- 背景支持 2 区或 4 区分割、常用角度吸附、区域点击选择，以及平整、手撕、漫画、锯齿边缘。
- 分割线可沿自身方向平移；4 区模式下两条线可分别拖动。背景图片也支持画布内移动、缩放与每次 90° 旋转。
- 修复多张动态背景分区在编辑预览中偶发串位的问题，预览与导出保持一致。

### 时间轴与交互

- 素材拖动时可对齐相邻轨道的起止点，并向所有素材显示对齐参考线；不会移动被参考的素材。
- 循环素材改用与动态背景一致的帧间细缝表达循环边界，移除突兀的循环分隔样式。
- 在任意素材或音频轨道上可上下滑动浏览时间轴；仅长按图层排序时锁定纵向列表。

## 1.01 (Build 3) — 2026-08-22

### 时间轴与循环

- 素材和贴纸支持拖动延长并循环播放，素材默认保持自身时长，贴纸默认铺满时间轴。
- 循环素材的首次播放入点与后续回绕起点分离，裁剪起点后仍能按完整源素材循环。
- 时间轴缩略图按实际播放顺序显示，选中素材时在循环边界顶部显示轻量弧形拼接标记。
- 拖动结束手柄时显示实时循环次数，并改善循环素材前后裁剪逻辑。
- 时间轴支持更紧凑的多轨布局、纵向滚动、图层长按排序和稳定的素材拖动。

### 帧编辑与播放

- 选中素材时只编辑当前素材帧，选中背景时编辑合成帧。
- 删除帧按播放方向使用相邻保留帧补位，支持正放逻辑并为倒放扩展预留处理。
- 播放完成后播放头回到起点，预览与导出继续使用统一的合成渲染逻辑。

### 素材库与作品

- 动态素材预览增加播放入口和播放状态日志。
- 作品支持主动保存、编辑、删除和重新打开继续编辑。
- 画布、播放控制、时间轴刻度和素材条视觉细节进一步压缩与统一。

## 1.0 (Build 2) — 2026-08-21

### 播放与渲染

- 改为完整合成帧驱动预览播放，多个视频、动图和贴纸可以同时稳定播放。
- 预览渲染与导出共用同一套合成逻辑，避免播放时素材互相抢占导致丢帧。
- 保留受控的 LRU 帧缓存，降低重复解码和内存持续增长风险。
- 播放结束后自动停止，不再循环播放。

### 时间轴

- 支持素材整体移动、左裁剪和右裁剪。
- 每次拖拽开始时固定操作模式和起始快照，避免拖拽过程中命中区域切换造成抖动。
- 右侧裁剪只改变素材的结束时间，左侧边界保持固定。
- 修复有效范围视觉绘制居中导致的“缩短 4 秒却显示缩短 8 秒”问题。
- 时间轴轨道改为紧凑布局，移除每行多余的白色背景。
- 时间轴在直接编辑素材时锁定滚动，避免整条轨道跟随手势平移。

### 编辑器界面

- 画布按屏幕宽度铺满，支持不同画幅比例并保持居中布局。
- 画布与时间轴层级、阴影和播放控制布局优化。
- 增加一键清空编辑内容功能，不会删除素材库中的素材。
- 设置页版本信息改为从 App Bundle 动态读取。

### 素材与贴纸

- 增加涂鸦贴纸分类及对应素材。
- 贴纸和元素时间轴支持帧缩略图展示。
- 动态素材预览和播放入口持续优化。
